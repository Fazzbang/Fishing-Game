# Assets folder

This is where image files (sprites, tiles, backgrounds) will go once we have
real pixel art. For now the game uses simple colored rectangles instead of
images, so this folder is empty except for this note.
